<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp"
    android:background="@android:color/black"
    tools:context=".PassDataListFragment" >

    <ExpandableListView
        android:id="@+id/pd_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:listSelector="@drawable/elv_bg_selector"
        android:groupIndicator="@drawable/elv_indicator_selector"
        android:dividerHeight="2dp"
        android:divider="@drawable/white_stroke_shape"
        />

</FrameLayout>
