<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.romanpulov.violetnote.view.BasicNoteEditActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/note_type_text"
        android:text="Note Type"
        android:textAppearance="@style/Text3"
        />


    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/note_type_spinner"
        android:layout_below="@+id/note_type_text"
        android:layout_alignLeft="@+id/note_type_text"
        android:entries="@array/ui_note_type_entries"
        android:spinnerMode="dropdown"
        android:popupBackground="@android:color/transparent"
        />

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@+id/note_type_spinner"
        >
        <RadioButton android:id="@+id/note_type_checked"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/list_selector"
            android:text="Checked"
            android:checked="true"
            android:textColor="@color/brightTextColor"
            android:theme="@style/RadioButton"
            />
        <RadioButton android:id="@+id/note_type_named"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/list_selector"
            android:text="Named"
            android:textColor="@color/brightTextColor"
            android:theme="@style/RadioButton"
            />
    </RadioGroup>

</RelativeLayout>
